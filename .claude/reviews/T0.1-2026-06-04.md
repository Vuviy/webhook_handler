# Review: T0.1 — Bootstrap skeleton & front controller — 2026-06-04

- **Reviewer:** code-reviewer
- **Scope:** app code is untracked (not in `git diff`), reviewed directly.
- **Spec:** `.claude/specs/webhook-handler.spec.md` · **ADR:** `.claude/decisions/0002-web-root-and-front-controller.md`

## Acceptance criteria (T0.1 scope)
- [x] PSR-4 (`App\` → `src/`) — `app/composer.json:15-19`. **PASS**
- [x] `app/public/index.php` front controller boots autoloader, returns health JSON. **PASS**
- [x] Nginx web root → `public/` (`docker/nginx/conf.d/default.conf:3`); `.env`/`composer.json`/`src/` not served. **PASS** (verified externally)
- [x] Old `app/index.php` removed — no dual entry point. **PASS**
- [x] PSR-4 proven end-to-end: `GET /` → `200` `{"status":"ok","service":"webhook-handler"}`. **PASS**

## Findings
| # | Severity | File:line | Issue | Resolution |
|---|----------|-----------|-------|------------|
| 1 | MINOR | `app/public/index.php` | No explicit HTTP status set | **Fixed** — added `http_response_code(200)` |
| 2 | NIT | `app/public/index.php` | `use` after `require` | **Fixed** — `use` moved above `require` |
| 3 | NIT | `docker/nginx/conf.d/default.conf` | Irregular indentation; no dotfile deny block | Deferred (cosmetic; root allow-list already protects) |
| 4 | NIT | `app/public/index.php` | `JSON_THROW_ON_ERROR` uncaught | Deferred to global error handler (later task) |

No BLOCKER/MAJOR. `declare(strict_types=1)` present; `Health` is `final`, fully typed; no secrets/crypto in scope.

## INFO (deferred, not T0.1 defects)
- `app/.env` keys don't match the config loader to come, and lack Redis/provider-secret keys → **T0.2**.
- `larapack/dd` sits in `require` (not `require-dev`) — tidy before any production-like milestone.

## Tooling
- **psalm:** not configured (no `psalm.xml`) — not run, reported honestly.
- **phpcs:** not configured (no `phpcs.xml`) — not run. Recommend adding both so the DoD gate becomes real.

## Verdict
**Approve with nits** — all nits MINOR/NIT; the two code nits were applied. T0.1 meets its full scope.
